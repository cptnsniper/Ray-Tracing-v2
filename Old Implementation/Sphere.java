public class Sphere
{
    Vector3 pos;
    double radius;
    Material mat;
    public Sphere(Vector3 pos, double radius, Material mat)
    {
        this.pos = pos;
        this.radius = radius;
        this.mat = mat;
    }
}